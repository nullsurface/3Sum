#ifndef _3SUM_H
#define _3SUM_H

// This returns all the unique three sums
int** threeSum(int*, int, int*, int**);

// This returns if the perticular triplet is unique in the return set
int isUnique(int**, int*, int, int ,int);
#endif

